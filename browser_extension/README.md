# Focus Buddy 网址桥接

浏览器不会把地址栏内容暴露给普通桌面程序，所以网址白名单需要这枚本地扩展。

## Edge 安装

1. 打开 `edge://extensions`。
2. 打开左侧“开发人员模式”。
3. 点击“加载解压缩的扩展”，选择项目或安装目录中的 `browser_extension` 文件夹。

Chrome 的步骤相同，入口是 `chrome://extensions`。

扩展只把当前活动标签页的**域名**和标题发送到 `127.0.0.1`。程序只在内存中
保留最新域名，不保存完整网址、路径或浏览历史；无痕窗口不会上报。
